export type TipoRequisicao = 'recarga' | 'venda_afiliados'

export interface RequisicaoBase {
  id: string
  user_id: number
  user_nome: string
  status: 'aguardando_adm' | 'aceita' | 'recusada' | 'aguardando_pagamento' | 'paga' | 'expirada'
  data_criacao: string
  adm_responsavel?: number
}

export interface RequisicaoRecarga extends RequisicaoBase {
  tipo?: 'recarga'
  valor: number
  chave_pix?: string
  data_aceite?: string
  data_pagamento?: string
}

export interface RequisicaoVendaAfiliados extends RequisicaoBase {
  tipo: 'venda_afiliados'
  quantidade: number
  valor: number
}

export type Requisicao = RequisicaoRecarga | RequisicaoVendaAfiliados